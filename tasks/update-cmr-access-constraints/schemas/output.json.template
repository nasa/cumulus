{
  "title": "UpdateCmrAccessConstraintsOutput",
  "description": "Describes the output produced by the update-cmr-access-constraints task",
  "type": "object",
  "properties": {
    "granules": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["granuleId", "files"],
        "properties": {
          "granuleId": {
            "type": "string"
          },
          "files": "{{files}}"
        }
      }
    }
  }
}
