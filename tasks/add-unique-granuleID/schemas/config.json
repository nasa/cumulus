{
  "title": "AddUniqueGranuleIDConfig",
  "description": "Describes the configuration the add-unique-granule-id task component",
  "type": "object",
  "properties": {
    "hashLength": {
      "description": "The maximum length (characters) of the computed granule hash",
      "type": ["null", "number", "string"],
      "default": 8
    },
    "includeTimestampHashKey": {
      "description": "Optional boolean config on whether the hash, which is collectionId, should include timestamp, defaults to false",
      "type": ["null", "boolean"],
      "default": "false"
    }
  }
}
