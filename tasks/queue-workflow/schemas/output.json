{
  "title": "QueueWorkflowOutput",
  "description": "Describes the output produced by the queue-workflow task",
  "type": "object",
  "required": [ "workflow", "workflowInput", "running" ],
  "additionalProperties": false,
  "properties": {
    "workflow": { "type": "string" },
    "workflowInput": {
      "type": "object"
    },
    "running": {
      "type": "string",
      "description": "a step function execution arn of workflow queued"
    }
  }
}
