{
  "name": "@cumulus/wait-for-deploy",
  "private": true,
  "version": "1.10.1",
  "description": "Wait for deployment.  For integration tests only, not publicly available",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "homepage": "https://github.com/nasa/cumulus/tree/master/tasks/waitForDeploy",
  "repository": {
    "type": "git",
    "url": "https://github.com/nasa/cumulus"
  },
  "engines": {
    "node": ">=8.10.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && webpack --mode=production",
    "prepublishOnly": "PRODUCTION=true npm run build"
  },
  "author": "Cumulus Authors",
  "license": "Apache-2.0",
  "dependencies": {
    "@cumulus/common": "^1.10.0",
    "p-retry": "^2.0.0"
  },
  "devDependencies": {
    "webpack": "~4.5.0",
    "webpack-cli": "~2.0.14"
  }
}
