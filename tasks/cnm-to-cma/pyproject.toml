[project]
name = "cnm2cma"
version = "0.1.0"
description = ""
authors = [
    { name = "ESHIN D Yen", email = "david.yen@jpl.nasa.gov" }
]
readme = "README.md"
requires-python = ">=3.12,<4.0"
dependencies = [
    "boto3-stubs (==1.41.5)",
    "cumulus-process==1.6.0"
]

[tool.poetry]
packages = [{ include = "cnm2cma", from = "src" }]

[dependency-groups]
dev = [
    "pytest (>=9.0.1,<10.0.0)"
]
