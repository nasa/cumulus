{
  "title": "LzardsBackupConfig",
  "description": "Describes config options for the LZARDS backup task",
  "type": "object",
  "properties": {
    "urlType": {
      "description": "The type of access URL to pass to LZARDS: S3 direct access OR distribution",
      "type": "string",
      "default": "s3",
      "enum": ["s3", "distribution"]
    },
    "distributionEndpoint": {
      "description": "Distribution endpoint URL/host. Required if urlType is distribution",
      "type": "string"
    }
  }
}