export enum tableNames {
  asyncOperations = 'async_operations',
  collections = 'collections',
  executions = 'executions',
  files = 'files',
  granules = 'granules',
  pdrs = 'pdrs',
  providers = 'providers',
  rules = 'rules',
}
