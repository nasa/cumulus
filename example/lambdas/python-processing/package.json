{
  "name": "@cumulus/python-process-activity",
  "private": true,
  "version": "21.0.1",
  "description": "Python reference activity",
  "homepage": "https://github.com/nasa/cumulus/tree/master/example/lambdas/python-reference-activity",
  "repository": {
    "type": "git",
    "url": "https://github.com/nasa/cumulus"
  },
  "scripts": {
    "test": "uv run python -m unittest",
    "python-lint": "uv run ruff check . && uv run ruff format --diff . && uv run mypy process_activity.py",
    "lint": "npm run python-lint",
    "clean": "rm -rf .venv",
    "build": "uv sync",
    "prepare": "npm run build",
    "package": "true",
    "install-python-deps": "npm run build"
  },
  "publishConfig": {
    "access": "private"
  },
  "nyc": {
    "exclude": [
      "tests"
    ]
  },
  "author": "Cumulus Authors",
  "license": "Apache-2.0"
}