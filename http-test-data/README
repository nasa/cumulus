The http container managed by docker-compose.yml will serve out any files
in this directory.  The intention is that any files being fetched using http
should be copied into this directory (probably under a subdirectory), fetched,
and then deleted.
